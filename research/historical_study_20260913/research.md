# Multi-year evaluation of all six ETH forecast horizons

The user's current request authorizes historical evaluation and the necessary implementation, instead of waiting months for live labels. This extends the existing separate-head candidate protocol; it does not turn a backtest into a record of historical publication.

Pinned code: main 4daef5ba5543de636c748843c109058744bc1b4a. Trusted full research state: successful same-repository/main push run 34595849686, commit ba038ed5c925109f34a1b03a2d2b17d6095720e1, artifact 10261568451, ZIP SHA256 6c4b314eb137976cc5dfa0cc2f36f69311ea4eb059bd53f412687c1acddd0832. The downloaded digest matches. It contains 169,851 ETH/BTC hourly rows spanning 2017-01-01 through 2026-09-11 11:00 UTC and 535 cached incumbent monthly fits. Missing candles are not filled.

The current incumbent already has 2018/2019-2026 chronological reconstruction: 2,637/2,621/2,595/2,536/2,259/2,101 origins for 6h/1d/3d/7d/14d/30d respectively. The 30d series has 71 nonoverlapping windows, 20.54 percentage-point return MAE against 19.71 for no change, and 79.01% coverage of a nominal 80% interval. Live settled sample counts reported previously do not describe this historical sample.

The concrete gap is that optimization.py::run_review evaluates its improved frozen-calibration/separate-head policy on only about 90 recent days. That is inadequate for long-horizon policy comparison across market cycles. It also provides uncertainty for event Brier only, while selecting point and interval outputs independently. Initial long-horizon training/calibration windows cannot be fabricated: with current source gaps and minimum rows, the actual first paired origin is 2020-10-06 for 14d and 2020-12-01 for 30d. Earlier requested months are explicitly excluded.

Reuse the current candidate family and settings: earlier training and selection, final refit, disjoint calibration and alert-threshold samples, then the next untouched-by-that-fit calendar month. Do not choose a new architecture after inspecting the test year. Earlier evaluated months may train later fits only once their full targets mature. The entire historical dataset is already development evidence in this project; it is not a newly untouched final holdout or original source-vintage replay.

The attached three papers were previously read and are summarized in research/horizon_optimization_20260911/research.md. They motivate chronological, output-specific comparisons but do not establish an ETH 30-day winning model. Primary methodological references: https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.TimeSeriesSplit.html and https://scikit-learn.org/stable/modules/calibration.html . A generic row gap alone is insufficient here: explicitly purge by target_end and preserve the exact model/calibrator identity.

Actual-data perturbation audit: with a 2024-01-01 cutoff, multiply only later OHLC prices by five and volume by three. Earlier feature values, matured truth, past-bound source cache keys, selected heads and every forecast output were exactly unchanged for both 6h and 30d. Separate refits produced different checkpoint identifiers; the audit compares forecast values and causal dependencies, not an unsupported claim of byte-identical independent fits. The existing saved-model round-trip test separately verifies model/calibrator identity after serialization. Reproduce with scripts/check_event_future_perturbation.py against the trusted full source state.

Implementation checkpoint: local repository regression suite passed 316 tests. Draft PR #43 contains the study, bounded resumable research worker, complete-report importer and chart integration. The historical run evaluates 486 requested horizon/month pairs (January 2020 through September 2026). Partial horizons are not a published complete study; final counts and outcome comparisons will be recorded after all pairs are processed.

Actual-data cache audit: the completed January 2024 6h month was reused without fitting or inference. A source revision after its final evaluated target left the exact cached result unchanged. A content-hash revision inside the evaluated future window invalidated only the outcome cache; a revision before the fit cutoff invalidated the fitted-model cache. The probes stopped at the expected cache miss and did not modify input data, saved models or issued records. Evidence: cache_audit.json; reproduction: scripts/check_event_historical_cache.py.

Concurrent main integration: PR #42 merged as fc1cf929a992fd8f20a85b4ee5047c7b31041fd6 during the study. Its logo, status, publishing and sponsorship changes are preserved. A three-way merge of index.html adds only the candidate-history source option and summary section to that latest page. The combined repository passed 319 tests in 45.81 seconds; incumbent training code and the ongoing study were unaffected.

Final actual-data outcome: all 486 requested horizon/month combinations completed; 435 months evaluated and 51 excluded. Candidate origin/nonoverlap counts are 2,172/2,172 (6h), 2,135/1,070 (24h), 2,090/527 (72h), 2,031/259 (168h), 1,977/135 (336h), and 1,901/64 (720h). Exact full output is results.json and its SHA256 is recorded in audit.json. The audit independently reconciles metrics, paired truth, complete target maturity, temporal partition boundaries, archive integrity and denominator counts. All 43 public evidence objects passed the importer. Original issued.db and shadow_active.json still match the original downloaded snapshot byte for byte.

Only the 6h interval output passes the registered exploratory historical screens: interval score improves from 7.3586 to 7.1558 percentage points (2.76%). Its price and probability outputs do not improve, so this is not a whole-model adoption result. Every other horizon fails the broad historical screens. The 30d candidate return MAE is 19.9914 pp, incumbent 20.0480 pp, and no-change 19.0247 pp; the candidate-minus-incumbent price-loss interval still crosses zero. Its nominal 80% interval covers only 70.12% of outcomes. No new model settings were selected after observing these test scores, and no production replacement is justified by this run.
