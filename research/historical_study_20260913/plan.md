# Registered implementation and evaluation plan

1. Add a resumable monthly candidate replay for all 6/24/72/168/336/720-hour horizons. Request origin months from 2020-01 onward, record each excluded month and reason, and use every eligible 00:00 UTC origin with a fully observed future target. Keep the original incumbent replay, active models and actual ledgers intact.
2. Reuse train_candidate without tuning its existing windows, model grid or calibration after observing test scores. Pair each candidate with the existing monthly incumbent, its frequency baseline and an explicit no-change point baseline on exactly the same dates. Validate training/selection/calibration/threshold boundaries before inference. Preserve fit/source/protocol hashes and original target dates. Cache fitted candidates separately from evaluated outcomes, with input hashes restricted to their actual time dependencies.
3. Save each completed month atomically and resume after a bounded budget. Publish a complete summary only after all requested horizon/month pairs are processed. Keep progress separate from the last complete report. Excluded initial-history and missing-input periods are explicit, not successful forecasts.
4. Report per-horizon first/last dates, calendar coverage, nonoverlapping windows, MAE/RMSE, no-change comparison, probability Brier/log loss, large-move detection, 80% coverage and interval score. Compare candidate-minus-reference loss with fixed calendar-block bootstrap intervals for point, probability and range; blocks span at least the full horizon. Add fixed calendar-year and origin-time state diagnostics; do not use the best test slice to select a winner.
5. Connect recurring historical research to the existing research artifacts, with a separate bounded job and resumable state, without delaying hourly inference. Attach the latest complete historical candidate report and content-addressed per-origin archive to future hourly publications and the existing chart controls. Vercel's confirmed cooldown remains in force.
6. Verify actual-data metrics, full target maturity, future-price/outcome perturbation invariance, hash-sensitive cache invalidation, resume behavior and failure-safe reporting. Run the repository CI before merging. A successful historical experiment is evidence for improvement proposals, not an automatic paid-service/live-superiority claim. If a candidate fails its primary baseline comparisons, retain the incumbent and report failure rather than claiming improvement.

Execution budget: two CPU threads, at most 1,500 seconds per invocation, persistent monthly checkpoints; initial multi-year completion may require resuming. Future runs reuse completed fits and evaluate only changed/new months. The existing candidate training remains weekly for short horizons and monthly for long horizons, with only fully matured outcomes. No retrospective insertion into issued.db and no production model selection from the displayed test years.

Publication and scheduling detail: a separate ETH multi-year candidate study workflow follows successful regular research, owns no site-writer lock, and retains resumable state even when the per-invocation budget ends. A partial study cannot produce the complete-report artifact. Same-repository PR and main runs share its concurrency group, permitting reviewed PR checkpoints to be reused after merge. The hourly worker downloads only the small, completed, same-repository/main report artifact and validated archive objects, never candidate history training files or ledgers. Its monthly source snapshot merger preserves independently imported historical evidence.
