# Execution and follow-up

PR #38 merged at `73c53976a7c7562353f27e9bc3a66131386a5f1e` after 298 tests passed locally and in GitHub CI run 34715371982. Production daily run 34715476316 completed successfully at 2026-09-12 19:55 UTC and preserved 15 normalized market/vendor CSV caches. The actual archived source partitions restored exactly to 1,918 latest source rows with original receipt times, values and hashes (1,920 source versions in the snapshot).

Production verification revealed two additional concrete issues:

1. A missing vendor measurement serialized as `NaN` in collector_summary.json. Strict JSON consumers cannot parse it. The collector now writes null for nonfinite/missing values, retaining the source error and keeping valid numeric measurements unchanged. Apply to the summary, quality audit and optional JSON stdout.
2. The merge's simultaneous daily/source/hourly triggers replaced pending workers under the existing shared publication concurrency group. The daily collector succeeded, but hourly was cancelled before any job started. Chain hourly after successful `Daily archived hybrid settlement`, the existing final daily writer. This adds no cycle: hourly completion only starts the watchdog, not the daily chain. The normal hourly schedule and idle/cooldown/deadline recovery controls remain. Source collector code/cache changes also explicitly trigger the daily workflow.

The daily run reports Binance HTTP 451 and a CoinGecko DeFi HTTP 429; these are missing auxiliary sources, not successful collection. No regional access bypass or synthetic filling was attempted. The current hourly model uses Coinbase bars independently.

The follow-up keeps model values, promotion gates and original records intact. Validate strict JSON handling of nested missing values, retain error explanations, rerun the required regression CI, and verify the successful daily chain then hourly archive/publication before claiming deployment completion.

The follow-up PR #39 passed 299 tests in CI run 34715831763 and merged at 27029f2bce90f8bc1a16975b2054b2b0b91621f0. Daily run 34715967557 restored all 15 saved caches, preserved them again, and emitted strict JSON (3,229 daily rows, 118 columns, last closed day 2026-09-11). Hourly run 34715967613 generated all six incumbent and six shadow records for 20:00 UTC, persisted 1,922 source versions over seven observation dates and 12 model checkpoints, and uploaded its recovery artifact. Its external publication verification failed: the site still returned the prior release.

GitHub's Vercel status for production commit a70a55baec35965adbee3f6a6b59fb100e63a4dd at 2026-09-12T20:06:31Z explicitly reports a deployment rate limit with a 24-hour wait. The same Vercel failure on data/event-ledger commit 9696330 proves audit-only pushes also reach the deployment integration. Do not treat successful inference, Git commits or artifacts as successful website delivery. No paid upgrade or alternative host is authorized by this incident fix.

Apply Vercel's documented git.deploymentEnabled branch filters: all branches disabled except data/daily-forecast; during the confirmed incident window, disable that branch as well. Keep collecting/persisting, leave missing external delivery receipts missing, and make the bounded recovery watchdog report hosting_cooldown without dispatching more producers. A fresh scheduled hourly run may resume publication at or after 2026-09-13T20:10:00Z (2026-09-14 05:10 KST); externally verify it before declaring recovery. Existing stale records are never retrospectively marked as delivered.

Primary configuration references: https://vercel.com/docs/project-configuration/git-configuration and https://vercel.com/docs/limits. Unspecified branches default to enabled; when patterns overlap, a matching true rule enables deployment. Skipped deployments are separate from deployment builds. This conservative incident window is anchored to the observed provider status and includes a small margin; actual resumption remains subject to the provider.
